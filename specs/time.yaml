openapi: 3.0.0
info:
  title: Time Demo API
  version: 1.0.0
  description: Stable GET sample used for the APIM demo.
servers:
  - url: https://postman-echo.com
paths:
  /get:
    get:
      summary: Return a stable echo payload
      operationId: getTime
      responses:
        '200':
          description: Payload returned successfully
          content:
            application/json:
              schema:
                type: object
